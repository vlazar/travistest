language: node_js

node_js:
  - "stable"

branches:
  only:
    - gh-pages

before_script:
  - export CHROME_BIN=chromium-browser
  - export DISPLAY=:99.0
  - sh -e /etc/init.d/xvfb start

deploy:
  provider: npm
  api_key: $NPM_TOKEN
  on:
    branch: gh-pages
